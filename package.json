{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build && pnpm --filter @workspace/db run push && npx esbuild api/index.js --bundle --platform=node --format=cjs --outfile=api/index.js --allow-overwrite --external:pg-native --external:esbuild --external:lightningcss"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
