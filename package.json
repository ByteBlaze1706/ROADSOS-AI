{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build && pnpm --filter @workspace/db run push"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
